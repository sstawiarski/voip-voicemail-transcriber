{
	"name": "voip_vm",
	"version": "1.0.0",
	"description": "",
	"main": "dist/src/index.js",
	"scripts": {
		"start": "npm run build && functions-framework --target=voicemail_handler --port=8080",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"zip": "npm run build && zip -r functions.zip dist/ && zip -g functions.zip {package.json,package-lock.json,tsconfig.json} && rm -rf dist/",
		"plan": "npm run zip && ./infra/deploy.sh plan production",
		"deploy:test": "npm run zip && ./scripts/deploy.sh apply test && rm functions.zip",
		"gcp-build": "npm run build"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@google-cloud/functions-framework": "^3.1.3",
		"@google-cloud/logging-winston": "^5.3.0",
		"@google-cloud/secret-manager": "^4.2.1",
		"@google-cloud/speech": "^5.4.0",
		"axios": "^1.3.4",
		"env-var": "^7.3.0",
		"iti": "^0.6.0",
		"winston": "^3.8.2"
	},
	"devDependencies": {
		"typescript": "^4.9.5"
	}
}
